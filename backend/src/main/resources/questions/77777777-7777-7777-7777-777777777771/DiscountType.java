/**
 * Supported discount types. Do not modify this file.
 */
public enum DiscountType {
    /** Percentage off the item price (e.g. 10% off). */
    PERCENTAGE,
    /** Buy One Get One: every second item is free. */
    BOGO,
    /** Fixed dollar amount off each item (e.g. $5 off). */
    FLAT
}
