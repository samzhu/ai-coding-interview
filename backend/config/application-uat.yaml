# =============================================================================
# UAT 環境行為配置
# =============================================================================
# Profile: uat
# 用途: User Acceptance Test，搭配 gcp 基礎設施 profile（spring.profiles.active=gcp,uat）
# =============================================================================

logging:
  level:
    root: INFO
